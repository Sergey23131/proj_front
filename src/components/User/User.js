export function User({info}) {
    return (
        <div className="User">
          <p>-name: {info.name}</p>
            <p> -nickName: {info.nickName}</p>
            <p> -age:{info.age}</p>
            <p> -role: {info.role}</p>
            SSS
        </div>
    );
}
